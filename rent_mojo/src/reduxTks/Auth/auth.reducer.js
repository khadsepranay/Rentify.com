const initState = {
	mobileNum: '',
	success: false,
	loading: false,
	error: false,
};

export const authReducer = (state = initState, { type, payload }) => {
	switch (type) {
		default: {
			return state;
		}
	}
};
